services:
  - type: web
    name: sri6he-flashcards-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
